#ifndef BWPRINTER_H
#define BWPRINTER_H
/*
#include <windows.h>
#include <bwstring.h>

class BWPrinter {
	public:
		BWPrinter();
		BWPrinter(const BWString &);
		~BWPrinter();
		
		void Open(const BWString &);
		void Close();
		void Pause();
		void Resume();
		void DeleteJob(int);
		void PurgeJobs();
		
		HANDLE GetHandler();
		
	private:
		BWString m_printer;
		HANDLE m_handler;
};
*/
#endif // BWPRINTER_H